This release removes Pi Banner's package-level Node.js engine restriction so compatibility follows Pi's supported runtime range.

## 🐞 Bug fixes

### Remove Node engine requirement

Pi Banner no longer declares its own Node.js engine requirement, allowing Pi's own supported Node.js range to determine compatibility.

*By @mavam.*
